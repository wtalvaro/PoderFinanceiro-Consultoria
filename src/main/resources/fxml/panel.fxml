<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<ScrollPane fitToWidth="true" fitToHeight="true"
    maxHeight="1.7976931348623157E308"
    style="-fx-background-color: transparent;"
    styleClass="edge-to-edge" hbarPolicy="NEVER" vbarPolicy="AS_NEEDED"
    minHeight="0"
    xmlns="http://javafx.com/javafx/21"
    xmlns:fx="http://javafx.com/fxml/1"
    fx:controller="br.com.poderfinanceiro.app.controller.PanelController">

    <VBox spacing="10" prefWidth="250" maxHeight="1.7976931348623157E308"
        style="-fx-background-color: -color-bg-default; -fx-padding: 15 10; -fx-border-color: -color-border-muted; -fx-border-width: 0 1 0 0;">

        <Label text="GERENCIAL"
            style="-fx-font-size: 11px; -fx-font-weight: bold; -fx-text-fill: -color-fg-muted; -fx-padding: 0 0 0 5;" />

        <Button fx:id="btnDashboard" text="📊 Visão Geral" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat, accent" onAction="#abrirWorkspace" />

        <Label text="VENDAS E CLIENTES"
            style="-fx-font-size: 11px; -fx-font-weight: bold; -fx-text-fill: -color-fg-muted; -fx-padding: 10 0 0 5;" />

        <Button fx:id="btnPlaybook" text="📖 Playbook de Vendas" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirPlaybook" />

        <Button fx:id="btnBaseClientes" text="👥 Base de Contatos/Clientes" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirTelaBaseClientes" />

        <Label text="ESTEIRA DE CRÉDITO"
            style="-fx-font-size: 11px; -fx-font-weight: bold; -fx-text-fill: -color-fg-muted; -fx-padding: 15 0 0 5;" />

        <Button fx:id="btnPropostas" text="📄 Propostas (Em Andamento)" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirPropostasList" />

        <Button fx:id="btnPendencias" text="⏳ Pendências / Exigências" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" style="-fx-text-fill: -color-danger-fg;"
            onAction="#abrirPendenciasList" />

        <Label text="REGRAS E CONSULTAS"
            style="-fx-font-size: 11px; -fx-font-weight: bold; -fx-text-fill: -color-fg-muted; -fx-padding: 20 0 0 5;" />

        <Button fx:id="btnBancosConvenios" text="🏦 Bancos e Convênios" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirBancosConvenios" />

        <Button fx:id="btnTabelasJuros" text="📈 Tabelas de Juros" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirTabelasJuros" />

        <Button fx:id="btnTabelaComissoes" text="💰 Gestão de Repasses (RV)" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirTabelaComissoes" />

        <Button fx:id="btnLinks" text="🔗 Links e Atalhos" maxWidth="Infinity"
            alignment="BASELINE_LEFT" styleClass="flat" onAction="#abrirLinksUteis" />

        <Region VBox.vgrow="ALWAYS" />
    </VBox>
</ScrollPane>